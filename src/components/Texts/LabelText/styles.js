import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    fontSize: 16,
  },
  lightText: {
    color: "#96abb1",
  },
  darkText: {
    color: "#dceaee",
  },
});

export default styles;
