import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "#23262e",
    fontSize: 24,
    lineHeight: 24 * 1.6,
    fontWeight: "bold",
    textAlign: "center",
  },
});

export default styles;
