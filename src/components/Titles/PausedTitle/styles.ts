import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    lineHeight: 24 * 1.6,
    fontWeight: "bold",
    textAlign: "center",
  },
  lightTitle: {
    color: "#23262e",
  },
  darkTitle: {
    color: "#dceaee",
  }
});

export default styles;
